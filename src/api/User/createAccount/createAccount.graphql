type Mutation{
    createAccount(
        username:String!
        email:String!
        firstName:String
        lastname:String
        bio:String
    ): User!
}

type Query {
    something: String!
}